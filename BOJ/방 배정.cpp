#include <iostream> 
#include <algorithm>
#include <string>
using namespace std;

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	// 학생의 수 N, 최대 인원 K
	// N줄만큼 성별(0은 여자 1은 남자), 학년(1~6)
	int n, k, arr[16] = {};
	int S, Y;
	cin >> n >> k;
	while (n-- > 0) {
		cin >> S >> Y;

	}
}